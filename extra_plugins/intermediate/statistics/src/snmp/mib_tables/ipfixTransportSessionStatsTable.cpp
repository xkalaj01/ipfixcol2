/*
 * Note: this file originally auto-generated by mib2c
 * using mib2c.table_data.conf
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "ipfixTransportSessionStatsTable.h"

struct cache_data{
    netsnmp_tdata                *table;
    TransportSessionStatsTable_t *storage;
};

/** Initialize the ipfixTransportSessionStatsTable table by defining its contents and how it's structured */
void
initialize_table_ipfixTransportSessionStatsTable(TransportSessionStatsTable_t *storage, uint data_timeout)
{
    const oid ipfixTransportSessionStatsTable_oid[] = {1,3,6,1,2,1,193,1,2,1};
    const size_t ipfixTransportSessionStatsTable_oid_len   = OID_LENGTH(ipfixTransportSessionStatsTable_oid);
    netsnmp_handler_registration    *reg;
    netsnmp_tdata                   *table_data;
    netsnmp_table_registration_info *table_info;
    netsnmp_cache                   *cache;
    struct cache_data               *data;

    DEBUGMSGTL(("ipfixTransportSessionStatsTable:init", "initializing table ipfixTransportSessionStatsTable\n"));

    reg = netsnmp_create_handler_registration(
              "ipfixTransportSessionStatsTable",     ipfixTransportSessionStatsTable_handler,
              ipfixTransportSessionStatsTable_oid, ipfixTransportSessionStatsTable_oid_len,
              HANDLER_CAN_RONLY
              );

    table_data = netsnmp_tdata_create_table( "ipfixTransportSessionStatsTable", 0 );
    if (NULL == table_data) {
        snmp_log(LOG_ERR,"error creating tdata table for ipfixTransportSessionStatsTable\n");
        return;
    }
    cache = netsnmp_cache_create(data_timeout,
                                  ipfixTransportSessionStatsTable_load, ipfixTransportSessionStatsTable_free,
                                  ipfixTransportSessionStatsTable_oid, ipfixTransportSessionStatsTable_oid_len);

    // Creating data for cache to read while reloading MIB
    data = static_cast<cache_data *>(malloc(sizeof(struct cache_data )));
    data->table = table_data;
    data->storage = storage;

    if (NULL == cache) {
        snmp_log(LOG_ERR,"error creating cache for ipfixTransportSessionStatsTable\n");
    }
    else{
        cache->flags = NETSNMP_CACHE_DONT_FREE_EXPIRED | NETSNMP_CACHE_AUTO_RELOAD;
        cache->magic = (void *)data;
    }
    table_info = SNMP_MALLOC_TYPEDEF( netsnmp_table_registration_info );
    if (NULL == table_info) {
        snmp_log(LOG_ERR,"error creating table info for ipfixTransportSessionStatsTable\n");
        return;
    }
    netsnmp_table_helper_add_indexes(table_info,
                           ASN_UNSIGNED,  /* index: ipfixTransportSessionIndex */
                           0);

    table_info->min_column = COLUMN_IPFIXTRANSPORTSESSION_RATE;
    table_info->max_column = COLUMN_IPFIXTRANSPORTSESSION_DISCONTINUITYTIME;
    
    netsnmp_tdata_register( reg, table_data, table_info );
    if (cache) 
        netsnmp_inject_handler_before( reg, netsnmp_cache_handler_get(cache),
                                       TABLE_TDATA_NAME);

    /* Initialise the contents of the table here */
}

/* create a new row in the table */
netsnmp_tdata_row *
ipfixTransportSessionStatsTable_createEntry(netsnmp_tdata *table_data
                 , u_long  ipfixTransportSessionIndex
                ) {
    TransportSessionStatsEntry_t *entry;
    netsnmp_tdata_row *row;

    entry = SNMP_MALLOC_TYPEDEF(TransportSessionStatsEntry_t);
    if (!entry)
        return NULL;

    row = netsnmp_tdata_create_row();
    if (!row) {
        SNMP_FREE(entry);
        return NULL;
    }
    row->data = entry;

    DEBUGMSGT(("ipfixTransportSessionStatsTable:entry:create", "row 0x%lu\n", (uintptr_t)row));
    entry->TransportSessionIndex = ipfixTransportSessionIndex;
    netsnmp_tdata_row_add_index( row, ASN_UNSIGNED,
                                 &(entry->TransportSessionIndex),
                                 sizeof(entry->TransportSessionIndex));
    if (table_data)
        netsnmp_tdata_add_row( table_data, row );
    return row;
}

/* remove a row from the table */
void
ipfixTransportSessionStatsTable_removeEntry(netsnmp_tdata     *table_data, 
                 netsnmp_tdata_row *row) {
    struct ipfixTransportSessionStatsTable_entry *entry;

    if (!row)
        return;    /* Nothing to remove */

    DEBUGMSGT(("ipfixTransportSessionStatsTable:entry:remove", "row 0x%lu\n", (uintptr_t)row));

    entry = (struct ipfixTransportSessionStatsTable_entry *)row->data;
    SNMP_FREE( entry );   /* XXX - release any other internal resources */

    if (table_data)
        netsnmp_tdata_remove_and_delete_row( table_data, row );
    else
        netsnmp_tdata_delete_row( row );    
}

/* Example cache handling - set up table_data list from a suitable file */
int
ipfixTransportSessionStatsTable_load( netsnmp_cache *cache, void *vmagic ) {
    (void) cache;
    netsnmp_tdata     *table;
    netsnmp_tdata_row *row;
    TransportSessionStatsEntry_t *mib_row;
    struct cache_data      *data;

    data = static_cast<cache_data *>(vmagic);
    table = data->table;

    while(storage_lock.test_and_set(std::memory_order_acquire));
    for ( auto it : *data->storage) {

        row = ipfixTransportSessionStatsTable_createEntry(table
                         , it.first
                        );
        if (row == NULL)
            continue;
        mib_row = (TransportSessionStatsEntry_t *)row->data;
        memcpy(mib_row, &it.second, sizeof(TransportSessionStatsEntry_t));
        mib_row->valid = 1;
    }
    storage_lock.clear(std::memory_order_release);
    return 0;  /* OK */
}

void
ipfixTransportSessionStatsTable_free( netsnmp_cache *cache, void *vmagic ) {
    (void) cache;
    struct cache_data *data = (struct cache_data*) vmagic;
    netsnmp_tdata     *table = data->table;
    netsnmp_tdata_row *mib_row;

    while ((mib_row = netsnmp_tdata_row_first(table))) {
        ipfixTransportSessionStatsTable_removeEntry(table, mib_row);
    }
}

/** handles requests for the ipfixTransportSessionStatsTable table */
int
ipfixTransportSessionStatsTable_handler(
    netsnmp_mib_handler               *handler,
    netsnmp_handler_registration      *reginfo,
    netsnmp_agent_request_info        *reqinfo,
    netsnmp_request_info              *requests) {

    (void) handler;
    (void) reginfo;

    netsnmp_request_info       *request;
    netsnmp_table_request_info *table_info;
    TransportSessionStatsEntry_t          *table_entry;

    struct counter64            c64;
    uint32_t                    timeticks = 0;

    DEBUGMSGTL(("ipfixTransportSessionStatsTable:handler", "Processing request (%d)\n", reqinfo->mode));

    switch (reqinfo->mode) {
        /*
         * Read-support (also covers GetNext requests)
         */
    case MODE_GET:
        for (request=requests; request; request=request->next) {
            if (request->processed)
                continue;

            table_entry = (TransportSessionStatsEntry_t *)
                              netsnmp_tdata_extract_entry(request);
            table_info  =     netsnmp_extract_table_info( request);
    
            switch (table_info->colnum) {
            case COLUMN_IPFIXTRANSPORTSESSION_RATE:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                snmp_set_var_typed_integer( request->requestvb, ASN_GAUGE,
                                            table_entry->Rate);
                break;
            case COLUMN_IPFIXTRANSPORTSESSION_PACKETS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                c64.low = table_entry->Packets & UINT32_MAX;
                c64.high = (table_entry->Packets >> 32) & UINT32_MAX;
                snmp_set_var_typed_value( request->requestvb, ASN_COUNTER64,
                                            &c64, sizeof(c64));
                break;
            case COLUMN_IPFIXTRANSPORTSESSION_BYTES:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                c64.low = table_entry->Bytes & UINT32_MAX;
                c64.high = (table_entry->Bytes >> 32) & UINT32_MAX;
                snmp_set_var_typed_value( request->requestvb, ASN_COUNTER64,
                                          &c64, sizeof(c64));
                break;
            case COLUMN_IPFIXTRANSPORTSESSION_MESSAGES:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                c64.low = table_entry->Messages & UINT32_MAX;
                c64.high = (table_entry->Messages >> 32) & UINT32_MAX;
                snmp_set_var_typed_value( request->requestvb, ASN_COUNTER64,
                                          &c64, sizeof(c64));
                break;
            case COLUMN_IPFIXTRANSPORTSESSION_DISCARDEDMESSAGES:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                c64.low = table_entry->DiscardedMessages & UINT32_MAX;
                c64.high = (table_entry->DiscardedMessages >> 32) & UINT32_MAX;
                snmp_set_var_typed_value( request->requestvb, ASN_COUNTER64,
                                          &c64, sizeof(c64));
                break;
            case COLUMN_IPFIXTRANSPORTSESSION_RECORDS:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                c64.low = table_entry->Records & UINT32_MAX;
                c64.high = (table_entry->Records >> 32) & UINT32_MAX;
                snmp_set_var_typed_value( request->requestvb, ASN_COUNTER64,
                                          &c64, sizeof(c64));
                break;
            case COLUMN_IPFIXTRANSPORTSESSION_TEMPLATES:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                c64.low = table_entry->Templates & UINT32_MAX;
                c64.high = (table_entry->Templates >> 32) & UINT32_MAX;
                snmp_set_var_typed_value( request->requestvb, ASN_COUNTER64,
                                          &c64, sizeof(c64));
                break;
            case COLUMN_IPFIXTRANSPORTSESSION_OPTIONSTEMPLATES:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                c64.low = table_entry->OptionsTemplates & UINT32_MAX;
                c64.high = (table_entry->OptionsTemplates >> 32) & UINT32_MAX;
                snmp_set_var_typed_value( request->requestvb, ASN_COUNTER64,
                                          &c64, sizeof(c64));
                break;
            case COLUMN_IPFIXTRANSPORTSESSION_DISCONTINUITYTIME:
                if ( !table_entry ) {
                    netsnmp_set_request_error(reqinfo, request,
                                              SNMP_NOSUCHINSTANCE);
                    continue;
                }
                if(table_entry->DiscontinuityTime != 0){
                    timeticks = (uint32_t) (difftime(time(nullptr),table_entry->DiscontinuityTime) *100);
                }
                snmp_set_var_typed_value( request->requestvb, ASN_TIMETICKS,
                        &timeticks, sizeof(timeticks));
                break;
            default:
                netsnmp_set_request_error(reqinfo, request,
                                          SNMP_NOSUCHOBJECT);
                break;
            }
        }
        break;

    }
    return SNMP_ERR_NOERROR;
}
